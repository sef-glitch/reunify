# =============================================================================
# REUNIFY WEB APP - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local and fill in the values
# See docs/ENV.md for detailed instructions on obtaining each value
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (Required)
# -----------------------------------------------------------------------------
# Neon PostgreSQL connection string
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=

# -----------------------------------------------------------------------------
# AUTHENTICATION (Required)
# -----------------------------------------------------------------------------
# Auth.js secret - generate with: openssl rand -base64 32
AUTH_SECRET=

# Auth.js URL - your app's base URL (used to determine secure cookie settings)
# Development: http://localhost:5173
# Production: https://your-domain.com
AUTH_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# CORS (Optional)
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins for CORS
# Leave empty to use default settings
CORS_ORIGINS=

# -----------------------------------------------------------------------------
# CREATE.XYZ PLATFORM (Required if using Create.xyz integrations)
# -----------------------------------------------------------------------------
# Project group ID from Create.xyz dashboard
NEXT_PUBLIC_PROJECT_GROUP_ID=

# Host header for Create.xyz API requests
NEXT_PUBLIC_CREATE_HOST=

# Create.xyz base URLs
NEXT_PUBLIC_CREATE_BASE_URL=https://www.create.xyz
NEXT_PUBLIC_CREATE_API_BASE_URL=

# Environment mode: DEVELOPMENT or PRODUCTION
NEXT_PUBLIC_CREATE_ENV=DEVELOPMENT
